ROLE:           This application is designed to play the role of a resource server 
                within the OAuth 2.0 security model.  It exposes a single REST API which
                returns information about the client application as well as the resource
                owner which made the call.

START PAGE:     /api/get_data


  